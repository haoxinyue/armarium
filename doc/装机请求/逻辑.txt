首先在装机工单表中填入基本的信息字段
工程师接单，也可以能是派单
装机过程中，工程师需要在和新建设备一样的页面（可以复用可以新建），新建tb_device一条记录，但是字段 usage_state = 2，安装中。

单子关闭完成后，需要把tb_device中的 usage_state这个字段 从 2->1，使用中。


注意，由于设备和安装工单是多对一的关系，也就是 设备表 里面有 安装工单ID，所以在新建设备的时候，要把安装工单ID，带过去。
