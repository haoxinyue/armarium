设备表添加字段
1.是否需要计量
2.保养周期：单位，天
3.下一次计量时间


新建表计量工单表 tb_metering_case

计量工单流程：
先在每个设备的信息中输入：是否要计量，计量周期，下一次计量时间， 这就是做计量计划

系统自动提前一个月生成计量工单（按照下一次计量时间）。一个工单对应一个设备。

计量工单在创建的时候至少包含如下字段：工单ID，工单主题，工单状态：待计量，设备ID

调度把这些工单分派给工程师
工程师的计量单的列表界面上有关单按钮。

第三方人员开始计量，计量单有些字段需要更新入库：计量数据（文本框），是否通过，结果的照片，计量时间（后端自动生成）。